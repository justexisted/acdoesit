// Copy this file to config.js and fill in your Supabase credentials.
// Create a table named 'leads' with columns:
// name (text), email (text), want (text), role (text), problem (text), price (text), created_at (timestamptz, default now())
// Ensure Row Level Security is ON and add an insert policy for anon if desired.

window.SUPABASE_URL = "https://YOUR-PROJECT.supabase.co";
window.SUPABASE_ANON_KEY = "YOUR_PUBLIC_ANON_KEY";
window.SUPABASE_TABLE = "leads";


